{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run Backend API",
			"type": "shell",
			"command": "cd backend && php artisan serve --host=127.0.0.1 --port=8000",
			"isBackground": true
		}
	]
}